[tool.poetry]
name = "code-guardian"
version = "1.0.0"
description = "Autonomous self-healing code review pipeline"

[tool.poetry.dependencies]
python = "^3.11"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
